<div id="home">
{{> navbar }}

{{!-- top content with carousel and post button --}}
    <div id="content" class="container">
      {{#if display}}
      {{> displayBlock}}
      {{else}}
      {{> postBlock}}
      {{/if}}
    </div>

{{!-- post button --}}
    {{!-- <div class="post-button-container">
      <a class="btn btn-primary" href="/postblock" role="button">POST</a>
    </div> --}}

{{!--post button animation--}}
<div class="post-button-container">
      <a href="/postblock" class="btn">
          <span class="btn-content">Post<span>
          <span class="icon"><i class="fas fa-file-import fa-2x" aria-hidden="true"></i></span>
      </a>
    </div>

</div>
